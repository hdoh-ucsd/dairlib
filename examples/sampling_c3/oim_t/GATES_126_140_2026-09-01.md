# xArm6 Full Sampling-C3+ gates 126–140 — 2026-09-01

These gates prevent the unexplained long dwell and wrong-direction corrective
contact observed near control step 528. They do not change solver selection,
ADMM/C3+ settings, horizon, sample counts, costs, random seeds, contact
parameters, task tolerances, controller defaults, or success criteria.

Baseline commit: `b27a22ede277a4c324140fa0953f0a40bba98437` on
`oim_c++_anything`, with the documented Gates 101–125 dirty changes retained.
The canonical configuration SHA-256 remains
`d11cd65efbcf6ac7c814a0b690cc76f9135d27a9f607f6f10dc7d9b26051b990`.

## Gate ledger

| Gate | Authority | Result and evidence |
|---:|---|---|
| 126 | Immutable step-528 replay separates saturation from control-state drift | PASS: qdot, torque, joint limits, and Jacobian conditioning were unsaturated; physical contact had already ended |
| 127 | Object-attached signed contact gap is a pure receipt | PASS: `EvaluateFullSamplingC3ContactDwellContinuation` recomputes the measured face in W |
| 128 | Nominal tip-hold error is independently reported | PASS: the receipt exposes measured 3-D target error without changing OSC gains |
| 129 | Real penetrating contact outranks nominal target error | PASS: regression preserves contact when gap is nonpositive even outside the activation ball |
| 130 | Dwell reads fresh object and tip state every update | PASS: both measurements are taken after each LCM update boundary |
| 131 | Separating-side hold divergence terminates dwell | PASS: canonical dwell ended at 144 updates with 27.6957 mm hold error and +0.0189308 mm signed gap |
| 132 | Contact loss suppresses the unexecuted push tail | PASS: `execute_progress_tail` requires `!progress_contact_lost` |
| 133 | Contact loss cannot earn productive credit | PASS: selected sample was quarantined as `measured_contact_loss`; `task_progress_cycle=FAIL` |
| 134 | Failed dwell requires measured release before replanning | PASS: `progress_rejection_release=PASS` at update 1494 |
| 135 | Recovery admission starts at the post-primary measured pose | PASS: both lateral and terminal receipts use `cycle_planning_pose`, not `progress_start_pose` |
| 136 | Recovery cannot borrow primary translation progress | PASS: strict incremental terminal descent is required before live IK |
| 137 | Recovery cannot borrow primary yaw progress | PASS: the same strict receipt independently requires orientation nonregression |
| 138 | Contact-only fallback cannot bypass incremental Pareto admission | PASS: all ranked candidates pass through `has_cycle_task_nonregression` before fallback selection |
| 139 | Measured terminal polarity is checked before lateral-reserve exit | PASS: physical −0.155381 mm translation response was rejected after 40 updates and cleared |
| 140 | Fresh unchanged 8,000-update physical fall-through is recorded honestly | PASS implementation/COMPLETE execution; strict physical terminal remains FAIL |

## Focused verification

```text
bazel test //examples/sampling_c3/oim_t:xarm6_full_sampling_c3plus_test \
           //examples/sampling_c3/oim_t:xarm6_open_table_test
result: PASS

bazel build //examples/sampling_c3/oim_t:xarm6_sim \
            //examples/sampling_c3/oim_t:xarm6_osc_controller \
            //examples/sampling_c3/oim_t:xarm6_sampling_c3_controller \
            //examples/sampling_c3/oim_t:oim_t_config_check
result: PASS

bazel-bin/examples/sampling_c3/oim_t/oim_t_config_check
result: PASS
```

The new deterministic regressions are
`ContactDwellRequiresMeasuredTipHoldAndFaceContinuation` and
`RecoveryPolarityUsesPostPrimaryPoseAndBothTerminalComponents`. Existing
terminal-descent and recovery tests remain unchanged and pass.

## Gate-140 physical receipt

The immutable canonical result is outside Git at
`/root/push_anything_ADMM/results/xarm6_gates126_140_8000_jnxZWC`. Its
manifest, receipt, source patch, simulator/OSC/planner logs, six-joint control
CSV, and raw telemetry preserve provenance. The earlier exploratory result at
`xarm6_gates126_140_8000_UgHedG` is retained and not treated as canonical.

```text
requested updates:                    8000 at 20 ms
updates used:                         2424
physical contact episodes:            4
primary dwell termination:            144 updates
dwell hold error / signed gap:         0.0276957 m / +0.0000189308 m
corrective prediction (x-y / yaw):     +0.00554026 m / +0.194096 rad
first measured recovery (x-y / yaw):  -0.000155381 m / +0.0120555 rad
wrong-polarity rejection latency:      40 response updates
contact clear and sample invalidation: PASS
terminal object pose:                 [0.378307, 0.372219, -0.0360253]
terminal translation error:           0.772224 m > 0.05 m
terminal orientation error:           3.10556 rad > 0.10 rad
open_table terminal:                  FAIL
classified exit:                      closed-loop handoff failed
```

This campaign prevents the previously observed long uncontrolled dwell and
prevents lateral recovery from hiding regressive terminal motion. It does not
make the scenario successful. The next gate is post-rejection acquisition
continuity: preserve the verified neutral-anchor recovery while obtaining a
new physically conformant progress acquisition from the bounded corridor.

