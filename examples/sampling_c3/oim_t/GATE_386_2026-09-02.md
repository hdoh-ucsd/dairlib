# xArm6 Gate 386: Physically-Authoritative Dwell with a Measured Bar

Date: 2026-09-02 (loop resumed on user authorization, cycle 9)  
Source commit: `b27a22ede277a4c324140fa0953f0a40bba98437` (dirty research
worktree)  
Authorization: the user selected Gate-381 option (b) — "define completeness
in physically joined units and recalibrate the bar against measured
joined-step distributions."

## Change

1. New canonical config key `physical_contact_dwell_steps: 638`, derived as
   floor(0.9 x 709): 709 is the minimum measured productive initial-push
   joined-step count across the four phys-instrumented exact-8000 receipts
   (709 b12e63, 726 983a16, 818 6cbd6f, 897 77a76a; joined rates
   0.70-0.92). This is the arc's first authorized config edit; the
   canonical SHA-256 becomes
   `622c2bd483b80fcd53ebbdd771eada8f5b436f051b493eea97bfb5bb5490d342`
   (pre-386 receipts remain pinned to `d11cd65e...`). Validated as
   `0 < physical_contact_dwell_steps <= successor_minimum_contact_steps`
   and asserted in `xarm6_open_table_test`.
2. Dwell counters re-gated with the mechanism validated in the Gate-381b
   arc: credit requires the full physical join (fresh + episode-active +
   on-face + inward polarity); engagement is physically gated; three
   consecutive transactions without fresh+active+face contact convert a
   geometric latch into contact loss (polarity gates credit only).
3. The five dwell-completeness comparisons (initial window and completeness,
   progress productive/regression/terminal-observed) read the physical bar.
   Response-maturity windows and pre-receipt budget reservations stay in
   period units (conservative; measured receipts refine them).

## Validation

`results/xarm6_gate386_physdwell_exact8000_b400a3` (+ D mirror + video,
final frame verified): `initial_contact_dwell=PASS response_steps=638` —
completeness in physically-joined units exactly at the bar, 15.1 mm
translation and 0.032 rad yaw of real progress, zero premature resets,
91% join rate. 6 cycles (3 PASS), zero posture REJECTs, terminal FAIL
0.737584 / 2.79352 (yaw progress 0.348, gate-385 class). The 7.12 s
phantom-dwell class measured in gates 375-379 is now structurally
impossible: unjoined transactions cannot credit, and sustained physical
absence ends the latch in three transactions. Completing the initial dwell
at 638 joined (~800 wall periods) rather than 1,000 geometric returns
about 200 updates per run.

## Remaining authorization queue

1. Recovery-vs-progress admission asymmetry (GATE_385 record) — every run
   still ends here with ~50% budget held.
2. Acquisition routing / speed-class throughput knobs.
