# xArm6 Gates 387-388: Recovery-Admission Reconciliation + Attribution Instrumentation

Date: 2026-09-02 (resumed loop, user-authorized option a)  
Source commit: `b27a22ede277a4c324140fa0953f0a40bba98437` (dirty research
worktree); config SHA `742aebcfaa6815adc9f803a7e8694c1b866aa14156df6c55b4faf35a4e882dd7`

## Gate 387 (user-authorized option a): recovery admission reconciled

The three recovery-admission call sites now pass the terminal tolerances
(0.05 m / 0.10 rad) as debt caps — the same allowance component-decomposed
progress transactions receive — instead of the successor minima
(0.002/0.002). The lateral-recovery receipt still gates the active purpose
and measured-response polarity rejection is unchanged.

Validated (`xarm6_gate387_recovadmit_exact8000_fe2a32`): a recovery contact
ADMITTED and physically executed for the first time in the arc (pre-387:
128/128 candidates rejected, zero recovery contacts ever). The measured
response was wrong-polarity and released by design — correct machinery.

## Gate 388 series: the next layers, instrumented and measured

1. Far-reach stranding (`387b` draw): all four preview-recovery fallbacks
   verticalize AT the stranded tip (infeasible at 0.72 m radius). Fix: a
   fifth home-ward neutral-retreat fallback (position-only traverse to the
   acquisition home anchor, then verticalize) mirroring the acquisition
   chain. Armed; not yet exercised by a draw.
2. Replan-window stranding (`388` draw, OPEN): during a ~25 s solve window
   the arm was walked to a behind-the-base posture by a then-unattributable
   posture stream; all 204 executable candidates subsequently failed
   preview from that pose. Instrumentation landed: every publish site now
   stamps `saved_traj.metadata.description` (LcmTrajectory's
   `get_metadata=false` had been silently discarding descriptions
   repo-wide), and the OSC receipt prints the publisher tag — the next
   occurrence self-identifies (`corrective_reposition:<phase>` includes the
   live phase name).
3. Selector NULL, now measured (`388c` draw): at the NULL event, 72 ranked
   candidates split 1 quarantined / 13 central-side / **36 polarity**
   (|normal_W.x| > 0.5) / 22 receipted rejections — and the run RECOVERED
   via seed replenishment and continued. The polarity pre-filter removes
   half the pool at moderate yaw by geometry.

## Decision queue (authorization required)

1. Recovery polarity pre-filter (`|normal_W.x| > 0.5` + sign): measured to
   remove 36/72 candidates; loosening it is a pre-registered-threshold
   change. Evidence above.
2. Acquisition routing / speed-class throughput knobs (standing).

Trial ledger through this gate: n=16, success_rate 0.0 (8,000-update budget
vs a ~60,000-update task per the controller's own sufficiency estimate),
pos_err 0.7421 +/- 0.0151, theta_err 2.7630 +/- 0.1361, exec 227.2 +/- 21.6 s.
All runs D-mirrored with hash verification.
